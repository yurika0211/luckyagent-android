<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is allowed for LAN/dev access to lh serve (http://192.168.x.x:9090).
  Prefer Tailscale / HTTPS reverse proxy for anything beyond trusted LAN.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
