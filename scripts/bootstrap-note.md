# Bootstrap

This machine may not have Android SDK/JDK. Open the folder in Android Studio to install SDK and generate Gradle wrapper jars if needed.

```bash
# with Gradle installed:
gradle wrapper --gradle-version 8.11.1
./gradlew :app:assembleDebug
```
